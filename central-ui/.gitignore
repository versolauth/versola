# Dependencies
node_modules/
# package-lock.json is intentionally committed so CI builds are reproducible
# via `npm ci`. yarn/pnpm lockfiles stay ignored — we use npm.
yarn.lock
pnpm-lock.yaml

# Build output
dist/
build/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Temporary files
*.tmp
.cache/

# Playwright
test-results/
playwright-report/
playwright/.cache/
