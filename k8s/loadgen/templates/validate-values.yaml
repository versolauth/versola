{{/*
Renders nothing. Exists so the value guards in _helpers.tpl run once per
render, before any resource is produced -- Helm evaluates a directory's
templates in reverse alphabetical order, and this file sorts last.
*/}}
{{- include "loadgen.checkTopLevelPlacement" . -}}
