{{- if and .Values.ingress.enabled (not .Values.ingress.gatewayAPI.enabled) }}
{{- if not .Values.ingress.className }}
{{- fail "ingress.className is required when ingress.enabled is true and gatewayAPI isn't -- an empty value falls back to the cluster's default IngressClass, and a cluster whose only controller is a stock ingress-nginx install has none (that chart doesn't mark its own class as default). The Ingress would render, look correct, and be adopted by no controller: ADDRESS stays empty and every request gets a bare 404, which reads as a routing bug rather than an unclaimed resource. Name the class explicitly, e.g. \"nginx\"." }}
{{- end }}
{{- $groups := include "versola.routeGroups" . | fromYaml }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "versola.fullname" . }}
  labels:
    {{- include "versola.labels" . | nindent 4 }}
  {{- with .Values.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- with .Values.ingress.className }}
  ingressClassName: {{ . }}
  {{- end }}
  {{- with .Values.ingress.tls }}
  tls:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  rules:
    {{- range $host := .Values.ingress.hosts }}
    - host: {{ $host.host | quote }}
      http:
        paths:
          {{- range $group := $host.routes }}
          {{- $paths := index $groups $group }}
          {{- if not $paths }}
          {{- fail (printf "ingress.hosts[%s]: unknown route group %q (known: %s)" $host.host $group (keys $groups | sortAlpha | join ", ")) }}
          {{- end }}
          {{- range $paths }}
          - path: {{ .path }}
            pathType: {{ .pathType }}
            backend:
              service:
                name: {{ .service }}
                port:
                  number: {{ .port }}
          {{- end }}
          {{- end }}
    {{- end }}
{{- end }}
