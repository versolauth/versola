target/
.vscode/
.DS_Store
.bsp/
.idea/
.metals/
.bloop/
**/metals.sbt
/images
idea.properties
.scala-build/
country/
dev/

# Node.js
node_modules/
# package-lock.json is intentionally committed (esp. central-ui/) so CI builds
# are reproducible via `npm ci`. yarn/pnpm lockfiles stay ignored — we use npm.
yarn.lock
pnpm-lock.yaml

# Playwright
test-results/
playwright-report/
playwright/.cache/

# Local development
.local/

# Generated forms (built from central-ui/forms via npm run build:forms)
central/src/main/resources/forms/

develop.txt
.scala-semantic/